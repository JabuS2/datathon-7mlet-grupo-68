# Port the MCP server listens on
PORT=8200
# HTTP path for the streamable-HTTP MCP endpoint
MCP_HTTP_PATH=/mcp
# Base URL of the FastAPI api_service (no trailing slash)
API_SERVICE_URL=http://localhost:8000
